# Dependencies
node_modules/

# Build output
_site/
.cache/
pagefind/

# Test / tooling output
coverage/
test-results/
playwright-report/
.lighthouseci/
*.log
npm-debug.log*

# Environment / secrets (repo will be public — never commit these)
.env
.env.*
!.env.example
.dev.vars
.wrangler/

# Migration scratch dir — SQL dumps contain unpublished/personal data; the whole
# folder is working-only. The reconciliation report is copied to docs/MIGRATION.md
# and redirects into src/_redirects. Never commit any of it (repo is public).
migration/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
